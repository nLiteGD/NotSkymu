<!--
    ============================================================
    Copyright © The Skymu Team and other contributors.
    For any inquiries or concerns, email contact@skymu.app.
    ============================================================
    Modification or redistribution of this code is governed
    by the terms set out in the project license agreement.
    If you do not comply with those terms, you may not
    modify or distribute any original code from the project.
    ============================================================
    License: https://skymu.app/legal/license
    SPDX-License-Identifier: AGPL-3.0-or-later
    ============================================================
-->
<Page
    x:Class="Skymu.Forms.OptionPages.General.Skymu"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:Skymu.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:enums="clr-namespace:Skymu.Enumerations"
    xmlns:global="clr-namespace:Skymu"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:preferences="clr-namespace:Skymu.Preferences"
    xmlns:theming="clr-namespace:Skymu.Theming"
    HorizontalAlignment="Stretch"
    VerticalAlignment="Stretch"
    DataContext="{StaticResource Lang}"
    FontFamily="Tahoma"
    FontSize="11"
    Foreground="{DynamicResource Text.VeryHighContrast}"
    Style="{StaticResource Page}"
    mc:Ignorable="d">
    <!--  Commented out as the page is a scrolling thing  -->
    <!--  Re-add to Page when the view becomes small enough to not require a ScrollViewer  -->
    <!--  d:Height="505"  -->
    <Page.Resources>
        <converters:EnumToBooleanConverter x:Key="EnumToBooleanConverter" />
        <converters:BoolInverterConverter x:Key="BoolInverterConverter" />
        <converters:StringToIntegerConverter x:Key="StringToIntegerConverter" />
    </Page.Resources>

    <ScrollViewer Margin="10">
        <StackPanel Orientation="Vertical">

            <StackPanel Margin="0,5" Orientation="Horizontal">
                <TextBlock
                    Padding="0,0,10,0"
                    VerticalAlignment="Center"
                    Text="Branding name" />
                <TextBox
                    x:Name="SC_BrandingNameBox"
                    Width="250"
                    FontFamily="Tahoma"
                    Text="{Binding Path=[BrandingName], Mode=TwoWay, Source={x:Static preferences:Settings.Default}, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>

            <StackPanel Margin="0,5" Orientation="Horizontal">
                <TextBlock
                    Padding="0,0,10,0"
                    VerticalAlignment="Center"
                    Text="{Binding [sF_USERINFO_LABEL_LANGUAGE]}" />
                <ComboBox
                    Width="250"
                    DisplayMemberPath="Key"
                    ItemsSource="{Binding Languages}"
                    SelectedValue="{Binding Path=[Language], Mode=TwoWay, Source={x:Static preferences:Settings.Default}}"
                    SelectedValuePath="Key" />
            </StackPanel>
            <StackPanel Margin="0,5" Orientation="Horizontal">
                <CheckBox
                    Padding="6,0"
                    Content="Follow system regional variances (time format, date format, units)"
                    Foreground="{DynamicResource Text.VeryHighContrast}"
                    IsChecked="{Binding Path=[UseSystemCulture], Mode=TwoWay, Source={x:Static preferences:Settings.Default}}" />
                <TextBlock
                    Margin="8,0,0,0"
                    VerticalAlignment="Center"
                    Foreground="{DynamicResource Text.MediumContrast}"
                    Text="(Requires restart)" />
            </StackPanel>
            <Rectangle
                Height="1"
                Margin="0,10,10,10"
                Fill="{DynamicResource Card.Border}" />

            <StackPanel Margin="0,5" Orientation="Horizontal">
                <TextBlock
                    Padding="0,0,10,0"
                    VerticalAlignment="Center"
                    Text="User interface" />
                <ComboBox
                    Width="250"
                    SelectedValue="{Binding Path=[Theme], Mode=TwoWay, Source={x:Static preferences:Settings.Default}}"
                    SelectedValuePath="Tag">
                    <ComboBoxItem
                        Padding="6,0"
                        Content="Skype 4"
                        Tag="Skype4" />
                    <ComboBoxItem
                        Padding="6,0"
                        Content="Skype 5"
                        Tag="Skype5" />
                    <ComboBoxItem
                        Padding="6,0"
                        Content="Skype 6"
                        Tag="Skype6" />
                    <ComboBoxItem
                        Padding="6,0"
                        Content="Skype 7 (experimental)"
                        Tag="Skype7" />
                </ComboBox>
                <TextBlock
                    Margin="8,0,0,0"
                    VerticalAlignment="Center"
                    Foreground="{DynamicResource Text.MediumContrast}"
                    Text="(Requires restart)" />
            </StackPanel>


            <StackPanel Margin="0,5" Orientation="Horizontal">
                <TextBlock
                    Padding="0,0,10,0"
                    VerticalAlignment="Center"
                    Text="Window frame" />
                <ComboBox
                    Width="250"
                    SelectedValue="{Binding Path=[WindowFrame], Mode=TwoWay, Source={x:Static preferences:Settings.Default}}"
                    SelectedValuePath="Tag">
                    <ComboBoxItem
                        Padding="6,0"
                        Content="Skype Aero"
                        Tag="{x:Static enums:WindowFrame.SkypeAero}" />

                    <ComboBoxItem
                        Padding="6,0"
                        Content="Skype Basic"
                        Tag="{x:Static enums:WindowFrame.SkypeBasic}" />

                    <ComboBoxItem
                        Padding="6,0"
                        Content="Use the system window frame"
                        Tag="{x:Static enums:WindowFrame.Native}" />
                </ComboBox>
                <TextBlock
                    Margin="8,0,0,0"
                    VerticalAlignment="Center"
                    Foreground="{DynamicResource Text.MediumContrast}"
                    Text="(Requires restart)" />
            </StackPanel>

            <StackPanel Margin="0,5" Orientation="Horizontal">
                <TextBlock
                    Padding="0,0,10,0"
                    VerticalAlignment="Center"
                    Text="Color theme" />
                <ComboBox
                    Width="250"
                    DisplayMemberPath="Key"
                    ItemsSource="{x:Static theming:Colorizer.Colorways}"
                    SelectedValue="{Binding Path=[Colorway], Mode=TwoWay, Source={x:Static preferences:Settings.Default}}"
                    SelectedValuePath="Key" />
                <TextBlock
                    Margin="8,0,0,0"
                    VerticalAlignment="Center"
                    Foreground="{DynamicResource Text.MediumContrast}"
                    Text="(Requires restart)" />
            </StackPanel>

            <StackPanel Margin="0,5" Orientation="Horizontal">
                <TextBlock
                    Padding="0,0,10,0"
                    VerticalAlignment="Center"
                    Text="Sound pack" />
                <ComboBox
                    x:Name="ComboSoundPack"
                    Width="250"
                    SelectedValue="{Binding Path=[SoundPack], Mode=TwoWay, Source={x:Static preferences:Settings.Default}}"
                    SelectedValuePath="Tag">

                    <ComboBoxItem
                        Padding="6,0"
                        Content="Enhanced"
                        Tag="{x:Static enums:Soundpack.Enhanced}" />

                    <ComboBoxItem
                        Padding="6,0"
                        Content="Skype 1 and 2"
                        Tag="{x:Static enums:Soundpack.Skype2}" />

                    <ComboBoxItem
                        Padding="6,0"
                        Content="Skype 3 to 7"
                        Tag="{x:Static enums:Soundpack.Skype7}" />

                    <ComboBoxItem
                        Padding="6,0"
                        Content="Skype 8"
                        Tag="{x:Static enums:Soundpack.Skype8}" />
                </ComboBox>
            </StackPanel>
            <StackPanel Margin="0,5" Orientation="Horizontal">
                <TextBlock
                    Padding="0,0,10,0"
                    VerticalAlignment="Center"
                    Text="Base theme (scrollbar, buttons, etc)" />
                <ComboBox
                    Width="250"
                    SelectedValue="{Binding Path=[PresentationFramework], Mode=TwoWay, Source={x:Static preferences:Settings.Default}}"
                    SelectedValuePath="Tag">
                    <ComboBoxItem
                        Padding="6,0"
                        Content="Windows Classic"
                        Tag="Classic" />
                    <ComboBoxItem
                        Padding="6,0"
                        Content="Windows XP (Blue)"
                        Tag="Luna.NormalColor" />
                    <ComboBoxItem
                        Padding="6,0"
                        Content="Windows XP (Olive Green)"
                        Tag="Luna.Homestead" />
                    <ComboBoxItem
                        Padding="6,0"
                        Content="Windows XP (Silver)"
                        Tag="Luna.Metallic" />
                    <ComboBoxItem
                        Padding="6,0"
                        Content="Windows XP Royale"
                        Tag="Royale.NormalColor" />
                    <ComboBoxItem
                        Padding="6,0"
                        Content="Windows Vista / 7"
                        Tag="Aero.NormalColor" />
                    <ComboBoxItem
                        Padding="6,0"
                        Content="Windows 8"
                        Tag="Aero2.NormalColor" />
                    <ComboBoxItem
                        Padding="6,0"
                        Content="Windows 8 Basic"
                        Tag="AeroLite.NormalColor" />
                    <ComboBoxItem
                        Padding="6,0"
                        Content="Windows 11 Dynamic (experimental)"
                        Tag="Fluent.Dynamic" />
                    <ComboBoxItem
                        Padding="6,0"
                        Content="Windows 11 High Contrast (experimental)"
                        Tag="Fluent.HC" />
                </ComboBox>
            </StackPanel>

            <CheckBox
                Margin="0,5"
                Padding="6,0"
                Content="Use the 'Classic Windows' Skype main color instead of the dynamic colors"
                Foreground="{DynamicResource Text.VeryHighContrast}"
                IsChecked="{Binding Path=[FallbackFillColors], Mode=TwoWay, Source={x:Static preferences:Settings.Default}}" />

            <CheckBox
                Margin="0,5"
                Padding="6,0"
                Content="Use the unreleased 'room-style' call UI from the mid-2012 Skype 5.5 mockups"
                Foreground="{DynamicResource Text.VeryHighContrast}"
                IsChecked="{Binding Path=[RoomCallUI], Mode=TwoWay, Source={x:Static preferences:Settings.Default}}" />

            <CheckBox
                Margin="0,5"
                Padding="6,0"
                Content="Use the call reconnecting sound instead of normal call out sound"
                Foreground="{DynamicResource Text.VeryHighContrast}"
                IsChecked="{Binding Path=[CallOutToReconnectSound], Mode=TwoWay, Source={x:Static preferences:Settings.Default}}" />

            <StackPanel Margin="0,5,0,10" Grid.IsSharedSizeScope="True">

                <Grid Margin="0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" SharedSizeGroup="FontRadioCol" />
                        <ColumnDefinition Width="Auto" SharedSizeGroup="FontLabelCol" />
                    </Grid.ColumnDefinitions>

                    <RadioButton
                        x:Name="UseDefaultFontRenderingRadio"
                        Grid.Column="0"
                        Margin="0,3,0,0"
                        VerticalAlignment="Top"
                        Checked="UseDefaultFontRenderingRadio_Checked"
                        GroupName="FontRenderingModeGroup" />

                    <StackPanel
                        Grid.Column="1"
                        Margin="5,1,10,0"
                        VerticalAlignment="Center">
                        <TextBlock Text="Use WPF's default grayscale font rendering" />
                    </StackPanel>
                </Grid>

                <Grid Margin="0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" SharedSizeGroup="FontRadioCol" />
                        <ColumnDefinition Width="Auto" SharedSizeGroup="FontLabelCol" />
                    </Grid.ColumnDefinitions>

                    <RadioButton
                        x:Name="UseClearTypeRadio"
                        Grid.Column="0"
                        Margin="0,3,0,0"
                        VerticalAlignment="Top"
                        Checked="UseClearTypeRadio_Checked"
                        GroupName="FontRenderingModeGroup" />

                    <StackPanel
                        Grid.Column="1"
                        Margin="5,1,10,0"
                        VerticalAlignment="Center">
                        <TextBlock Text="Use accurate ClearType font rendering " />
                    </StackPanel>
                </Grid>

            </StackPanel>

            <CheckBox
                Margin="0,5"
                Padding="6,0"
                Content="Replace all dialog icons with 'Niko' from the game 'Oneshot'"
                Foreground="{DynamicResource Text.VeryHighContrast}"
                IsChecked="{Binding Path=[NikoIcons], Mode=TwoWay, Source={x:Static preferences:Settings.Default}}" />

            <Rectangle
                Height="1"
                Margin="0,10,10,10"
                Fill="{DynamicResource Card.Border}" />

            <CheckBox
                Margin="0,5"
                Padding="6,0"
                Content="Remember positioning (window size, location, sidebar size)"
                Foreground="{DynamicResource Text.VeryHighContrast}"
                IsChecked="{Binding Path=[SaveWindowPosition], Mode=TwoWay, Source={x:Static preferences:Settings.Default}}" />



            <StackPanel Margin="0,5,0,5" Grid.IsSharedSizeScope="True">
                <Grid Margin="0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" SharedSizeGroup="SidebarRadioCol" />
                        <ColumnDefinition Width="Auto" SharedSizeGroup="SidebarLabelCol" />
                    </Grid.ColumnDefinitions>

                    <RadioButton
                        x:Name="StaticSidebarTabsRadio"
                        Grid.Column="0"
                        Margin="0,3,0,0"
                        VerticalAlignment="Top"
                        Checked="StaticSidebarTabsRadio_Checked"
                        GroupName="SidebarTabSizeModeGroup" />
                    <StackPanel
                        Grid.Column="1"
                        Margin="5,1,10,0"
                        VerticalAlignment="Center">
                        <TextBlock Text="Static sidebar tabs: do not resize tab width based on selection state" />
                    </StackPanel>
                </Grid>
                <Grid Margin="0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" SharedSizeGroup="SidebarRadioCol" />
                        <ColumnDefinition Width="Auto" SharedSizeGroup="SidebarLabelCol" />
                    </Grid.ColumnDefinitions>

                    <RadioButton
                        x:Name="DynamicSidebarTabsRadio"
                        Grid.Column="0"
                        Margin="0,3,0,0"
                        VerticalAlignment="Top"
                        Checked="DynamicSidebarTabsRadio_Checked"
                        GroupName="SidebarTabSizeModeGroup" />

                    <StackPanel
                        Grid.Column="1"
                        Margin="5,1,10,0"
                        VerticalAlignment="Center">
                        <TextBlock Text="Accurate sidebar tabs: dynamically resize width based on selection state" />
                    </StackPanel>
                </Grid>

            </StackPanel>

            <Rectangle
                Height="1"
                Margin="0,10,10,10"
                Fill="{DynamicResource Card.Border}" />


            <CheckBox
                Margin="0,5"
                Padding="6,0"
                Content="Save credentials to file (credentials.xml)"
                Foreground="{DynamicResource Text.VeryHighContrast}"
                IsChecked="{Binding Path=[SaveCredentials], Mode=TwoWay, Source={x:Static preferences:Settings.Default}}" />

            <CheckBox
                Margin="0,5"
                Padding="6,0"
                Content="Enable automatic login if credentials are saved"
                Foreground="{DynamicResource Text.VeryHighContrast}"
                IsChecked="{Binding Path=[AutoLogin], Mode=TwoWay, Source={x:Static preferences:Settings.Default}}" />



            <StackPanel Margin="0,5" Orientation="Horizontal">
                <TextBlock
                    Padding="0,0,10,0"
                    VerticalAlignment="Center"
                    Text="Amount of messages to fetch during initial conversation load" />
                <TextBox
                    Width="40"
                    FontFamily="Tahoma"
                    Text="{Binding [MsgLoadCount], Converter={StaticResource StringToIntegerConverter}, Mode=TwoWay, Source={x:Static preferences:Settings.Default}, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>

            <CheckBox
                Margin="0,5"
                Padding="6,0"
                Content="Start with the window minimized"
                Foreground="{DynamicResource Text.VeryHighContrast}"
                IsChecked="{Binding Path=[StartMinimized], Mode=TwoWay, Source={x:Static preferences:Settings.Default}}" />
            <CheckBox
                Margin="0,5"
                Padding="6,0"
                Content="Allow multiple instances of the application to run at the same time (disabling this is experimental)"
                Foreground="{DynamicResource Text.VeryHighContrast}"
                IsChecked="{Binding Path=[AllowMultipleInstances], Mode=TwoWay, Source={x:Static preferences:Settings.Default}}" />
            <CheckBox
                Margin="0,5"
                Padding="6,0"
                Content="Run the speed test on each startup (uses 10 MB of bandwidth for no real benefit)"
                Foreground="{DynamicResource Text.VeryHighContrast}"
                IsChecked="{Binding Path=[AutoSpeedTest], Mode=TwoWay, Source={x:Static preferences:Settings.Default}}" />
            <CheckBox
                Margin="0,5"
                Padding="6,0"
                Content="{Binding [sF_USERENTRY_CB_START_SKYPE]}"
                Foreground="{DynamicResource Text.VeryHighContrast}"
                IsChecked="{Binding Path=[AutoLaunch], Mode=TwoWay, Source={x:Static preferences:Settings.Default}}" />
            <CheckBox
                Margin="0,5"
                Padding="6,0"
                Content="Retain original versions of edited and deleted messages"
                Foreground="{DynamicResource Text.VeryHighContrast}"
                IsChecked="{Binding Path=[StoreMessageHistory], Mode=TwoWay, Source={x:Static preferences:Settings.Default}}" />

            <Rectangle
                Height="1"
                Margin="0,10,10,10"
                Fill="{DynamicResource Card.Border}" />


            <StackPanel Margin="0,5" Orientation="Horizontal">
                <TextBlock
                    Padding="0,0,10,0"
                    VerticalAlignment="Center"
                    Text="Amount of Skype Credit (visual only)" />
                <TextBox
                    Width="80"
                    FontFamily="Tahoma"
                    Text="{Binding Path=[CredsText], Mode=TwoWay, Source={x:Static preferences:Settings.Default}, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>

            <StackPanel Margin="0,5" Orientation="Horizontal">
                <TextBlock
                    Padding="0,0,10,0"
                    VerticalAlignment="Center"
                    Text="Amount of Skype subscriptions (visual only)" />
                <TextBox
                    Width="40"
                    FontFamily="Tahoma"
                    Text="{Binding [CredsSubCount], Converter={StaticResource StringToIntegerConverter}, Mode=TwoWay, Source={x:Static preferences:Settings.Default}, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>

            <Rectangle
                Height="1"
                Margin="0,10,10,10"
                Fill="{DynamicResource Card.Border}" />


            <CheckBox
                Margin="0,5"
                Padding="6,0"
                Content="Enable notifications"
                Foreground="{DynamicResource Text.VeryHighContrast}"
                IsChecked="{Binding Path=[EnableNotifications], Mode=TwoWay, Source={x:Static preferences:Settings.Default}}" />
            <StackPanel Margin="0,5" Orientation="Horizontal">
                <TextBlock
                    Padding="0,0,10,0"
                    VerticalAlignment="Center"
                    Text="Display a notification for" />
                <ComboBox
                    Width="250"
                    SelectedValue="{Binding Path=[NotificationTrigger], Mode=TwoWay, Source={x:Static preferences:Settings.Default}}"
                    SelectedValuePath="Tag">
                    <ComboBoxItem
                        Padding="6,0"
                        Content="all recieved messages"
                        Tag="{x:Static enums:NotificationTriggerType.ALL}" />
                    <ComboBoxItem
                        Padding="6,0"
                        Content="direct messages, replies, and pings"
                        Tag="{x:Static enums:NotificationTriggerType.PDM}" />
                    <ComboBoxItem
                        Padding="6,0"
                        Content="replies and pings"
                        Tag="{x:Static enums:NotificationTriggerType.PING}" />
                    <ComboBoxItem
                        Padding="6,0"
                        Content="direct messages"
                        Tag="{x:Static enums:NotificationTriggerType.DM}" />
                </ComboBox>
            </StackPanel>
            <CheckBox
                Margin="0,5"
                Padding="6,0"
                Content="Force active notifications to be blue instead of the accurate orange"
                Foreground="{DynamicResource Text.VeryHighContrast}"
                IsChecked="{Binding Path=[BlueNotifications], Mode=TwoWay, Source={x:Static preferences:Settings.Default}}" />

            <Rectangle
                Height="1"
                Margin="0,10,10,10"
                Fill="{DynamicResource Card.Border}" />

            <CheckBox
                Margin="0,5"
                Padding="6,0"
                Content="Enable the 'Skype Home' feature (experimental, may use 20-60 MB of additional RAM)"
                Foreground="{DynamicResource Text.VeryHighContrast}"
                IsChecked="{Binding Path=[EnableSkypeHome], Mode=TwoWay, Source={x:Static preferences:Settings.Default}}" />
            <CheckBox
                Margin="0,5"
                Padding="6,0"
                Content="Do not display community ads while using Skype Home"
                Foreground="{DynamicResource Text.VeryHighContrast}"
                IsChecked="{Binding Path=[EnableAdBlock], Mode=TwoWay, Source={x:Static preferences:Settings.Default}}" />


            <Rectangle
                Height="1"
                Margin="0,10,10,10"
                Fill="{DynamicResource Card.Border}" />


            <CheckBox
                Margin="0,5"
                Padding="6,0"
                Content="Anonymize my username and display name in Skymu's connected user statistics"
                Foreground="{DynamicResource Text.VeryHighContrast}"
                IsChecked="{Binding Path=[Anonymize], Mode=TwoWay, Source={x:Static preferences:Settings.Default}}" />

            <CheckBox
                MaxWidth="450"
                Margin="0,5"
                HorizontalAlignment="Left"
                Foreground="{DynamicResource Text.VeryHighContrast}"
                IsChecked="{Binding Path=[BlockSkymuServerConnections], Mode=TwoWay, Source={x:Static preferences:Settings.Default}}">
                <TextBlock Margin="2,0" TextWrapping="Wrap"><Run Text="Completely block communication with the Skymu server and the updater (warning: not recommended, doing this breaks features in the Skymu client!)" /></TextBlock>
            </CheckBox>

            <Rectangle
                Height="1"
                Margin="0,10,10,10"
                Fill="{DynamicResource Card.Border}" />

            <StackPanel Grid.IsSharedSizeScope="True">

                <Grid Margin="0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" SharedSizeGroup="RadioCol" />
                        <ColumnDefinition Width="Auto" SharedSizeGroup="LabelCol" />
                    </Grid.ColumnDefinitions>

                    <RadioButton
                        x:Name="UseEmbeddedCert"
                        Grid.Column="0"
                        Margin="0,3,0,0"
                        VerticalAlignment="Top"
                        Checked="UseEmbeddedCert_Checked"
                        GroupName="CertSourceGroup" />

                    <StackPanel
                        Grid.Column="1"
                        Margin="5,1,10,0"
                        VerticalAlignment="Center">
                        <TextBlock Text="Use embedded certificate store (recommended)" />
                    </StackPanel>
                </Grid>

                <Grid Margin="0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" SharedSizeGroup="RadioCol" />
                        <ColumnDefinition Width="Auto" SharedSizeGroup="LabelCol" />
                    </Grid.ColumnDefinitions>

                    <RadioButton
                        x:Name="UseSystemCert"
                        Grid.Column="0"
                        Margin="0,3,0,0"
                        VerticalAlignment="Top"
                        Checked="UseSystemCert_Checked"
                        GroupName="CertSourceGroup" />

                    <StackPanel
                        Grid.Column="1"
                        Margin="5,1,10,0"
                        VerticalAlignment="Center">
                        <TextBlock Text="Use system certificate store" />
                    </StackPanel>
                </Grid>

                <Grid Margin="0,5,0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" SharedSizeGroup="RadioCol" />
                        <ColumnDefinition Width="Auto" SharedSizeGroup="LabelCol" />
                    </Grid.ColumnDefinitions>

                    <RadioButton
                        x:Name="UseCustomCert"
                        Grid.Column="0"
                        Margin="0,3,0,0"
                        VerticalAlignment="Top"
                        Checked="UseCustomCert_Checked"
                        GroupName="CertSourceGroup" />

                    <StackPanel
                        Grid.Column="1"
                        Margin="5,1,10,0"
                        VerticalAlignment="Center"
                        Orientation="Vertical">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Use custom certificate store (cacert.pem)" />
                        </StackPanel>

                        <StackPanel Margin="0,7,0,0" Orientation="Horizontal">
                            <TextBox
                                x:Name="SC_CertPathBox"
                                Width="300"
                                Height="23"
                                VerticalContentAlignment="Center"
                                IsEnabled="{Binding Path=[CertificateStore], Source={x:Static preferences:Settings.Default}, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Custom}"
                                Text="{Binding Path=[CertPath], Mode=TwoWay, Source={x:Static preferences:Settings.Default}, UpdateSourceTrigger=PropertyChanged}" />

                            <Button
                                Margin="5,0,0,0"
                                Padding="10,0"
                                Click="CertBrowseButtonClick"
                                Content="Browse..."
                                IsEnabled="{Binding Path=[CertificateStore], Source={x:Static preferences:Settings.Default}, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Custom}" />
                        </StackPanel>


                    </StackPanel>
                </Grid>

            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</Page>
